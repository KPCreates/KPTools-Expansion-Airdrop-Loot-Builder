# KPTools – Expansion Airdrop Loot Builder

A fast, user-friendly GUI tool for editing **DayZ Expansion AirdropSettings.json**.

Built to eliminate manual classname copying and JSON errors while working with Expansion airdrops.

---

## ✨ Features

- Browse **multiple types.xml files at once**
- Search and filter items by source file
- Add items to airdrops with one click
- Fast, non-freezing loot table (handles large files)
- Clone, duplicate, and remove airdrops
- Edit container-level settings:
  - ItemCount
  - Infected / InfectedCount
- Changes apply **only when Save is clicked**
- Automatic backup on save
- Clean **KPTools-style dark UI**
- Info tab with Discord support link

---

## 🧰 Requirements (Script Version)

- Python 3.10+
- Modules:
  - customtkinter
  - pillow

Install dependencies:
```bash
py -m pip install customtkinter pillow
